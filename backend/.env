PORT=5000  # Can remain as-is, but not needed for most cloud providers.
ATLAS_URI=<your-mongodb-atlas-connection-uri>  # Replace with your MongoDB Atlas URI.
JWT_SECRET=<your-jwt-secret>  # Replace with your secure JWT secret key.
FRONTEND_URL=<your-frontend-production-url>  # Replace with the production URL of your frontend, e.g., https://your-frontend-url.netlify.app.
